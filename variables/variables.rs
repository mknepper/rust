// Our main function.
fn main() {
    // "let" declares we're using a variable, "x" being our variable.
    let x = 3;
    // We set x to equal 3. Below we print x, using "{}" for x's input.
    println!("{}", x)
}
