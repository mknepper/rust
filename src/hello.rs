// Our main function which we'll put our code inside.
fn main() {
// The println! code prints "Hello world!" to our terminal.
    println!("Hello world!");
// We use a semi1colon to specify the end of the instruction.
}
// This final brack closes the function's parameters and ends the main fuction.
